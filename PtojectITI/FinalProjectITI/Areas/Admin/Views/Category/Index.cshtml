@model IEnumerable<FinalProjectITI.Models.Category>

@{
    ViewData["Title"] = "Index";
}



<p>
    <a class="btn btn-success" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th scope="col">
                @Html.DisplayNameFor(model => model.Category_Name)
            </th>
            <th scope="col">
                @Html.DisplayNameFor(model => model.Category_Describtion)
            </th>
            <th scope="col">Operation</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td scope="row">
                @Html.DisplayFor(modelItem => item.Category_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category_Describtion)
            </td>
            <td>
                <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.Category_ID">Edit</a> |
                <a class="btn btn-success" asp-action="Details" asp-route-id="@item.Category_ID">Details</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Category_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
